# Public configuration only. Never commit signing keys or bid secrets.
CHAIN_ID=1301
RPC_URL=https://sepolia.unichain.org
# Deployment scripts must derive the proceeds recipient from the actual signer.
# Use an encrypted Foundry keystore for signing.
